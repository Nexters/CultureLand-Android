import { connect } from 'react-redux';
import template from './signTemplate';

function mapStateToProps(state){

    return {

    }
}

const mapDispatchToProps = {

};

export default connect(mapStateToProps,mapDispatchToProps)(template);
